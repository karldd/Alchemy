<?xml version="1.0" encoding="UTF-8"?>
<!-- You may freely edit this file. See commented blocks below for -->
<!-- some examples of how to customize the build. -->
<!-- (If you delete it and reopen the project it will be recreated.) -->
<project name="Alchemy" default="default" basedir=".">
    <description>Builds, tests, and runs the project Alchemy.</description>
    
    <!--<import file="nbproject/profiler-build-impl.xml"/>-->
    
    <import file="nbproject/build-impl.xml"/>
    
    <import file="nbproject/profiler-build-impl.xml"/>    
    <!--

    There exist several targets which are by default empty and which can be 
    used for execution of your tasks. These targets are usually executed 
    before and after some main targets. They are: 

      -pre-init:                 called before initialization of project properties
      -post-init:                called after initialization of project properties
      -pre-compile:              called before javac compilation
      -post-compile:             called after javac compilation
      -pre-compile-single:       called before javac compilation of single file
      -post-compile-single:      called after javac compilation of single file
      -pre-compile-test:         called before javac compilation of JUnit tests
      -post-compile-test:        called after javac compilation of JUnit tests
      -pre-compile-test-single:  called before javac compilation of single JUnit test
      -post-compile-test-single: called after javac compilation of single JUunit test
      -pre-jar:                  called before JAR building
      -post-jar:                 called after JAR building
      -post-clean:               called after cleaning build products

    (Targets beginning with '-' are not intended to be called on their own.)

    Example of inserting an obfuscator after compilation could look like this:

        <target name="-post-compile">
            <obfuscate>
                <fileset dir="${build.classes.dir}"/>
            </obfuscate>
        </target>

    For list of available properties check the imported 
    nbproject/build-impl.xml file. 


    Another way to customize the build is by overriding existing main targets.
    The targets of interest are: 

      -init-macrodef-javac:     defines macro for javac compilation
      -init-macrodef-junit:     defines macro for junit execution
      -init-macrodef-debug:     defines macro for class debugging
      -init-macrodef-java:      defines macro for class execution
      -do-jar-with-manifest:    JAR building (if you are using a manifest)
      -do-jar-without-manifest: JAR building (if you are not using a manifest)
      run:                      execution of project 
      -javadoc-build:           Javadoc generation
      test-report:              JUnit report generation

    An example of overriding the target for project execution could look like this:

        <target name="run" depends="Alchemy-impl.jar">
            <exec dir="bin" executable="launcher.exe">
                <arg file="${dist.jar}"/>
            </exec>
        </target>

    Notice that the overridden target depends on the jar target and not only on 
    the compile target as the regular run target does. Again, for a list of available 
    properties which you can use, check the target you are overriding in the
    nbproject/build-impl.xml file. 

    -->    
    
    <!-- Copy the required libraries into the build file -->
    <target name="-pre-jar"> 
        <unjar src="${file.reference.commons-logging.jar}" dest="${build.classes.dir}"/> 
        <unjar src="${file.reference.itext.jar}" dest="${build.classes.dir}"/> 
        <unjar src="${file.reference.jpf.jar}" dest="${build.classes.dir}"/> 
        <unjar src="${file.reference.foxtrot.jar}" dest="${build.classes.dir}"/> 
        <unjar src="${file.reference.PDFRenderer.jar}" dest="${build.classes.dir}"/> 
    </target> 
    
    
    <target name="-post-jar">
        
        <!-- Copy the compiled Plugin classes  - Need to find a better way to do this! -->
        <copy file="${build.classes.dir}/org/alchemy/core/AlcCore.class" todir="${basedir}/modules/core/org/alchemy"/>
        
        
        <!-- CREATES -->
        <!-- Shapes -->
        <copy file="${build.classes.dir}/org/alchemy/create/Shapes.class" todir="${basedir}/modules/shapes/org/alchemy/create"/>
        <copy file="${build.classes.dir}/org/alchemy/create/Shapes$1.class" todir="${basedir}/modules/shapes/org/alchemy/create"/>
        
        <!-- Type Shapes -->
        <copy todir="${basedir}/modules/typeShapes/org/alchemy/create">
            <fileset dir="${build.classes.dir}/org/alchemy/create/" includes="**/*.class">
                <contains text="TypeShapes" casesensitive="no"/>
            </fileset>
        </copy>
        <!-- Inverse Shapes -->
        <copy todir="${basedir}/modules/inverseShapes/org/alchemy/create">
            <fileset dir="${build.classes.dir}/org/alchemy/create/" includes="**/*.class">
                <contains text="InverseShapes" casesensitive="no"/>
            </fileset>
        </copy>
        <!-- Microphone Shapes -->
        <copy todir="${basedir}/modules/micShapes/org/alchemy/create">
            <fileset dir="${build.classes.dir}/org/alchemy/create/" includes="**/*.class">
                <contains text="MicShapes" casesensitive="no"/>
            </fileset>
        </copy>
        <!-- Median Shapes -->
        <copy todir="${basedir}/modules/medianShapes/org/alchemy/create">
            <fileset dir="${build.classes.dir}/org/alchemy/create/" includes="**/*.class">
                <contains text="MedianShapes" casesensitive="no"/>
            </fileset>
        </copy>
        <!-- Speed Shapes -->
        <copy todir="${basedir}/modules/speedShapes/org/alchemy/create">
            <fileset dir="${build.classes.dir}/org/alchemy/create/" includes="**/*.class">
                <contains text="SpeedShapes" casesensitive="no"/>
            </fileset>
        </copy>
        <!-- X Shapes -->
        <copy todir="${basedir}/modules/xShapes/org/alchemy/create">
            <fileset dir="${build.classes.dir}/org/alchemy/create/" includes="**/*.class">
                <contains text="XShapes" casesensitive="no"/>
            </fileset>
        </copy>
        <!-- Trace Shapes -->
        <copy todir="${basedir}/modules/traceShapes/org/alchemy/create">
            <fileset dir="${build.classes.dir}/org/alchemy/create/" includes="**/*.class">
                <contains text="TraceShape" casesensitive="no"/>
            </fileset>
            <fileset dir="${build.classes.dir}/org/alchemy/create/" includes="**/*.class">
                <contains text="Flickr" casesensitive="no"/>
            </fileset>
        </copy>
        <!-- Detach Shapes -->
        <copy todir="${basedir}/modules/detachShapes/org/alchemy/create">
            <fileset dir="${build.classes.dir}/org/alchemy/create/" includes="**/*.class">
                <contains text="DetachShapes" casesensitive="no"/>
            </fileset>
        </copy>
        <!-- Scrawl Shapes -->
        <copy todir="${basedir}/modules/scrawlShapes/org/alchemy/create">
            <fileset dir="${build.classes.dir}/org/alchemy/create/" includes="**/*.class">
                <contains text="ScrawlShapes" casesensitive="no"/>
            </fileset>
        </copy>
        <!-- Camera Colour -->
        <copy todir="${basedir}/modules/cameraColour/org/alchemy/create">
            <fileset dir="${build.classes.dir}/org/alchemy/create/" includes="**/*.class">
                <contains text="CameraColour" casesensitive="no"/>
            </fileset>
        </copy>
        
        
        <!-- AFFECTS -->
        <!-- Mirror -->
        <copy todir="${basedir}/modules/mirror/org/alchemy/affect">
            <fileset dir="${build.classes.dir}/org/alchemy/affect/" includes="**/*.class">
                <contains text="Mirror" casesensitive="no"/>
            </fileset>
        </copy>
        <!-- Blindness -->
        <copy todir="${basedir}/modules/blindness/org/alchemy/affect">
            <fileset dir="${build.classes.dir}/org/alchemy/affect/" includes="**/*.class">
                <contains text="Blindness" casesensitive="no"/>
            </fileset>
        </copy>
        <!-- Random -->
        <copy todir="${basedir}/modules/random/org/alchemy/affect">
            <fileset dir="${build.classes.dir}/org/alchemy/affect/" includes="**/*.class">
                <contains text="Random" casesensitive="no"/>
            </fileset>
        </copy>
        <!-- Mic Expand -->
        <copy todir="${basedir}/modules/micExpand/org/alchemy/affect">
            <fileset dir="${build.classes.dir}/org/alchemy/affect/" includes="**/*.class">
                <contains text="MicExpand" casesensitive="no"/>
            </fileset>
        </copy>
        
        <!-- Repeat -->
        <copy todir="${basedir}/modules/repeat/org/alchemy/affect">
            <fileset dir="${build.classes.dir}/org/alchemy/affect/" includes="**/*.class">
                <contains text="Repeat" casesensitive="no"/>
            </fileset>
        </copy>
        <!-- Displace -->
        <copy todir="${basedir}/modules/displace/org/alchemy/affect">
            <fileset dir="${build.classes.dir}/org/alchemy/affect/" includes="**/*.class">
                <contains text="Displace" casesensitive="no"/>
            </fileset>
        </copy>
        <!-- Camera Colour -->
        <copy todir="${basedir}/modules/cameraColour/org/alchemy/create">
            <fileset dir="${build.classes.dir}/org/alchemy/create/" includes="**/*.class">
                <contains text="CameraColour" casesensitive="no"/>
            </fileset>
        </copy>
        
        <!--
        <move file="${build.classes.dir}/org/alchemy/create/ImageShapes.class" todir="${basedir}/modules/imageShapes/alchemy/create"/>
        -->

        <!-- Copy the Data folder to the Dist directory -->
        <!--
        <copy todir="${dist.dir}/data">
            <fileset dir="${basedir}/data"/>
        </copy>
         -->
         
        <!-- Copy the lib folder to the Dist directory
        <copy todir="${dist.dir}/lib">
            <fileset dir="${basedir}/lib"/>
        </copy>
         -->
         
        <!-- Define custom JPF specific Ant tasks -->
        <path id="classpath">
            <fileset dir="lib" includes="*.jar" />
        </path>
        
        <typedef resource="org/java/plugin/tools/ant/jpf-tasks.properties">
            <classpath refid="classpath" />
        </typedef>	
        
        <!-- Call "jpf-zip" task to process plug-ins packaging every plug-in as single ZIP file -->
        <jpf-zip
            basedir="${basedir}/modules"
            includes="*/*.xml"
            destdir="${basedir}/modules"/>
        
        <mkdir dir="${dist.dir}/modules" />
        
        <copy todir="${dist.dir}/modules">
            <fileset dir="${basedir}/modules">
                <include name="**/*.zip"/>
                <exclude name="**/*core*"/>
            </fileset>
        </copy>     
        
        
        
        <!-- Unzip and remove the plugin class files from the Alchemy Jar -->
        <unzip src="${dist.jar}" dest="${dist.dir}/Alchemy" />
        <delete includeemptydirs="true">
            <fileset dir="${dist.dir}/Alchemy/org/alchemy/affect" includes="**/*"/>
            <fileset dir="${dist.dir}/Alchemy/org/alchemy/create" includes="**/*"/>
        </delete>
        <!-- Delete the original jar -->
        <delete dir="${dist.jar}"/>
        <!-- 
        Unless we zip up the contents to a directory other than 'dist', 
        the .class files get automatically included.
        To get around this we make the zip in the main directory than move it.
        Hacky I know...
        -->
        <zip destfile="Alchemy.jar" basedir="${dist.dir}/Alchemy"/>
        <move file="Alchemy.jar" todir="${dist.dir}"/>
        
        <!-- Delete the extracted zip folder and other Netbeans crap while we are at it -->
        <delete dir="${dist.dir}/Alchemy"/>
        <delete dir="${dist.dir}/lib"/>
        <delete file="${dist.dir}/README.TXT"/>
        
        <!-- Copy the manual into the dist directory -->
        <copy file="${basedir}/Alchemy.pdf" todir="${dist.dir}"/>
        
        <!-- MAC STUFF 
             Not inlcuded in the distribution jar
             but put in the Mac Alchemy.app -->
        <!-- Copy the Quaqua libraries for more mac looking interface support -->
        <copy file="${basedir}/lib/libquaqua.jnilib" todir="${dist.dir}"/>
        <copy file="${basedir}/lib/quaqua.jar" todir="${dist.dir}/lib"/>
        
        
    </target>	
    
    
    
</project>
